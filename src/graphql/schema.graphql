### This file was generated by Nexus Schema
### Do not make changes to this file directly


"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

input DynamicPlaylistConfig {
  customParameters: customParameters
  itemsPerPage: Int
  pageNumber: Int
  sort: Sort
  tags: Tags
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON @specifiedBy(url: "http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf")

type Media {
  description: String
  kind: String
  playlist: [MediaPlaylistItem]
  title: String!
}

type MediaPlaylistItem {
  duration: Float!
  image: String
  images: [JSON]
  mediaid: String!
  pubDate: DateTime!
  sources: [JSON]
  tags: String
  title: String!
  tracks: [JSON]
}

type Mutation {
  createPlaylist(dynamicPlaylistConfig: DynamicPlaylistConfig!, playlistMetadata: PlaylistMetadata!): JSON
}

type Playlist {
  customParameters: JSON
  description: String
  feedid: String!
  playlist: [Media]
  title: String!
}

input PlaylistMetadata {
  customParameters: JSON
  description: String
  title: String
  type: String
}

type Query {
  getPlaylistById(playlistId: ID!): JSON
  media(id: ID!): JSON
}

input Sort {
  field: String
  order: String
}

input Tags {
  exclude: String
  include: String
}

input customParameters {
  exclude: JSON
  include: JSON
}